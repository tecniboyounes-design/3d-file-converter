# ============================================================
# 3D File Converter - Environment Configuration
# ============================================================
# Copy this file to .env and adjust values as needed

# Server Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=production
LOG_LEVEL=info

# File Handling
UPLOAD_DIR=./data/uploads
MAX_FILE_SIZE=104857600  # 100MB in bytes

# Conversion Settings
CONVERSION_TIMEOUT=300000  # 5 minutes in milliseconds

# Concurrency Limits (CRITICAL for preventing OOM crashes)
# Blender uses ~400MB RAM per process, Assimp uses ~50MB
MAX_CONCURRENT_BLENDER=2
MAX_CONCURRENT_ASSIMP=5

# Cleanup Settings
CLEANUP_INTERVAL_MS=60000   # Check every 1 minute
FILE_AGE_LIMIT_MS=120000    # Delete files older than 2 minutes

# ============================================================
# Autodesk Platform Services (APS) - Optional
# ============================================================
# Required for converting DXF files with ACIS 3D solids
# Get credentials at: https://aps.autodesk.com/myapps/
# Create an app with "Data Management API" and "Model Derivative API" enabled
# Use 2-legged OAuth (client credentials)
#
# APS_CLIENT_ID=your_client_id_here
# APS_CLIENT_SECRET=your_client_secret_here
# APS_BUCKET_KEY=tecnibo_3d_converter  # Optional, defaults to 'tecnibo_3d_converter'
